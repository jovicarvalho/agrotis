import TextField, { type TextFieldProps } from '@mui/material/TextField';

const AgrotisTextField: React.FC<TextFieldProps> = (props) => {
  return <TextField {...props} />;
};

export default AgrotisTextField;
